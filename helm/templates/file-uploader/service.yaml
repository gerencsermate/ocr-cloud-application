apiVersion: v1
kind: Service
metadata:
  name: file-uploader
spec:
  type: LoadBalancer
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: file-uploader
